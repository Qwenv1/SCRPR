# Required: Admin token for API authentication (must match the one in your Netlify frontend)
ADMIN_TOKEN=your-secret-token-here

# Optional: Set to "true" to enable Camoufox stealth browser (requires install)
USE_CAMOUFOX=false
